# Copy to terraform.tfvars and fill in, or set DIGITALOCEAN_TOKEN / TF_VAR_do_token
# in your shell instead of putting the token in a file.

# do_token = ""

ssh_allowed_ips = ["203.0.113.4/32"]

# Default droplet_size is now "gpu-6000adax1-48gb" - RTX 4000 Ada
# (gpu-4000adax1-20gb) currently has zero available regions anywhere,
# confirmed via the live DigitalOcean sizes/regions APIs. Re-verify before
# apply, and switch back if RTX 4000 Ada becomes available again:
# droplet_size = "gpu-6000adax1-48gb" # `doctl compute size list`
# image        = ""                    # `doctl compute image list --public`
