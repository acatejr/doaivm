# Copy to terraform.tfvars and fill in, or set DIGITALOCEAN_TOKEN / TF_VAR_do_token
# in your shell instead of putting the token in a file.

# do_token = ""

ssh_allowed_ips = ["203.0.113.4/32"]

# Default droplet_size is now "gpu-h100x1-80gb" ($4.41/hr) - the entire 48GB
# tier and RTX 4000 Ada are all at zero available regions right now,
# confirmed via the live DigitalOcean sizes/regions APIs. GPU capacity here
# shifts within minutes - re-verify immediately before apply, and switch to
# a cheaper tier the moment one is orderable again:
# droplet_size = "gpu-h100x1-80gb" # `doctl compute size list`
